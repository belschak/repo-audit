# Working scratch: hostile third-party clones under battle-tests/targets/ must
# never be committed, and the raw run notes stay local. Published, sanitized
# example audits live in docs/examples/.
battle-tests/

# Local QA / publish-gate evidence (references internal review process); kept
# for the maintainer's own review, not part of the published skill.
gates/

# Local launch drafts (Reddit/X post texts); posted manually, never shipped.
launch-notes/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
